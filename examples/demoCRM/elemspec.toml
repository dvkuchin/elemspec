[project]
name = "DemoCRM vendor example"
tests_dir = "tests"
reports_dir = "_reports"
state_dir = ".elemspec"
gaps_dir = "engine-gaps"

[hosts]
# Замените шаблонные hostname на hostname тестового приложения и его сервера
# авторизации. В allowlist не должно быть производственных окружений.
allowed = [
  "app.example.test",
  "auth.example.test",
]
